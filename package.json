{
  "name": "kraftkurve",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:shell": "pnpm -C apps/web ng serve app-shell --port 4200",
    "build:shell": "pnpm -C apps/web build",
    "dev:training": "pnpm -C apps/web ng serve app-training --port 4202",
    "build:training": "pnpm -C apps/web ng build app-training",
    "dev:admin": "pnpm -C apps/web ng serve app-admin --port 4201",
    "build:admin": "pnpm -C apps/web ng build app-admin",
    "dev:api": "pnpm -C apps/api dev",
    "build:api": "pnpm -C apps/api build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
